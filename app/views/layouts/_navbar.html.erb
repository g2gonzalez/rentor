<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
    <div class="navbar-header">
    	<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapsed">
    		<span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
			<a class="navbar-brand" href="/">Rentor</a>
    </div>

    <div class="collapse navbar-collapse" id="collapsed">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
          <li><p class="navbar-text">Signed in as <%= current_user.name %></p></li>
          <li><%= link_to "Logout", '/logout' %></li>
        <% else %>
          <li><%= link_to "Signup", signup_path %></li>
          <li><%= link_to "Login", login_path %></li>
        <% end %>
      </ul>
    </div>
	</div><!-- container -->
</nav>